# Лабораторні роботи з програмування (1 курс)

Програма для виконання 5 лабораторних робіт з програмування на Python з графічним інтерфейсом Tkinter.

## 📋 Вимоги

- Python 3.7 або новіша версія
- Tkinter (входить у стандартну поставку Python, але на деяких системах потрібне додаткове встановлення)

## 🚀 Швидкий старт

### Автоматична ініціалізація (рекомендовано)

```bash
# Надайте права на виконання скрипту
chmod +x setup.sh

# Запустіть скрипт ініціалізації
./setup.sh
```

Скрипт автоматично:
- Перевірить наявність Python
- Встановить Python та tkinter (якщо потрібно)
- Перевірить наявність всіх необхідних файлів
- Протестує середовище

### Ручне встановлення

#### macOS

```bash
# Встановлення Python з tkinter через Homebrew
brew install python@3.14 python-tk@3.14

# Або якщо використовуєте системний Python
# tkinter зазвичай вже встановлено
```

#### Linux (Ubuntu/Debian)

```bash
sudo apt-get update
sudo apt-get install python3 python3-tk
```

#### Linux (CentOS/RHEL)

```bash
sudo yum install python3 python3-tkinter
```

## 🎮 Запуск програми

```bash
python3 lab_works.py
```

Або якщо використовуєте конкретну версію Python:

```bash
python3.14 lab_works.py
```

## 📚 Опис лабораторних робіт

### Лабораторна робота №1
Розрахунок залишку коштів для різних варіантів покупок. Програма визначає, які варіанти покупок доступні для вказаної суми коштів.

### Лабораторна робота №2
Обчислення найбільшого спільного дільника (НСД) двох чисел за допомогою алгоритму Евкліда.

### Лабораторна робота №3
Знаходження простих чисел у заданому діапазоні. Користувач може вказати початкове та кінцеве значення діапазону.

### Лабораторна робота №4
Робота з файлами: збереження даних користувача (ім'я та дата народження) у файли та відображення їх вмісту.

### Лабораторна робота №5
Генерація та сортування комплексних чисел. Програма:
- Генерує 20 випадкових комплексних чисел
- Сортує їх за модулем
- Візуалізує на комплексній площині
- Виводить список усіх чисел

## 🛠️ Технології

- **Python 3** - мова програмування
- **Tkinter** - графічний інтерфейс
- **PIL/Pillow** (опціонально) - для роботи з зображеннями

## 📁 Структура проекту

```
tr-12-labs/
├── lab_works.py    # Головний файл програми
├── zstu.png        # Логотип ЗДТУ
├── tr12.png        # Логотип ТР-12
├── setup.sh        # Скрипт ініціалізації середовища
└── README.md       # Документація
```

## ⚙️ Налаштування

Програма автоматично знаходить зображення `zstu.png` та `tr12.png` в тій же директорії, що й `lab_works.py`.

## 🐛 Вирішення проблем

### Помилка: "No module named '_tkinter'"

**macOS:**
```bash
brew install python-tk@3.14
```

**Linux:**
```bash
sudo apt-get install python3-tk  # Ubuntu/Debian
sudo yum install python3-tkinter  # CentOS/RHEL
```

### Помилка: "ModuleNotFoundError: No module named 'tkinter'"

Переконайтеся, що використовуєте правильну версію Python:
```bash
python3 --version
python3 -c "import tkinter; print('tkinter OK')"
```

## 📝 Ліцензія

Цей проект створено для навчальних цілей.

## 👤 Автор

Лабораторні роботи з програмування, 1 курс
Грудень 2016

